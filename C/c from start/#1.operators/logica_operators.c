#include<stdio.h>
int main()
{
    int a= 4 ;
   
   // and (&&)
    printf("%d\n", (a>0) && (a<10) );

   // or (||)
   printf("%d\n", 3>4 || 5>2)  ;

// not  ( ! )
    printf("%d", !(3>2));

    return 0;
}